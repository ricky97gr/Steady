[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "quant-collector"
version = "0.1.0"
description = "A股数据采集服务（AkShare / Tushare）"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
