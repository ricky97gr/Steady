[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "quant-engine"
version = "0.1.0"
description = "A股量化引擎（因子计算 / 策略 / 回测）"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
